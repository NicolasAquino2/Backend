<!-- En tu archivo realTimeProducts.handlebars -->
<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Resto de las etiquetas head ... -->
</head>
<body>
  <h1>Real-Time Products</h1>
<script src="/socket.io/socket.io.js"></script>
  <table>
    <thead>
      <tr>
        <th>Título</th>
        <th>Precio</th>
        <th>Categoría</th>
        <!-- Agrega más encabezados de columna si es necesario -->
      </tr>
    </thead>
    <tbody>
      <!-- Agrega un identificador al tbody para actualizar el contenido -->
      <tbody id="productList">
        {{#each products}}
        <tr>
          <td>{{this.title}}</td>
          <td>{{this.price}}</td>
          <td>{{this.category}}</td>
          <!-- Agrega más celdas si es necesario -->
        </tr>
        {{/each}}
      </tbody>
    </tbody>
  </table>

  <!-- Agrega la etiqueta script para incluir el archivo socket.js -->
<!-- Agrega la etiqueta script para incluir el archivo socket.io.js -->

<script src="/public/js/realTimeProduts.js"></script>
<script src="/js/socket.js"></script>


  
</body>
</html>
